import os.path
import tempfile

script_folder = os.path.dirname(os.path.abspath(__file__)) + os.path.sep + "keops"
build_folder  = tempfile.mkdtemp(prefix="pykeops-") + os.path.sep

dll_prefix = "lib"
dll_ext = ".so"
