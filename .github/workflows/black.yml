name: Format Check

on:
  push:
  pull_request:

jobs:
  format:
    name: Format (Black)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Black format check
        uses: psf/black@stable
        with:
          options: "--check ."

