# chmod 700 compile
# ./compile "Scal<Square<Scalprod<X<2,4>,Y<3,4>>>,GaussKernel<P<0>,X<0,3>,Y<1,3>,Y<4,3>>>"
# ./compile "GaussKernel<P<0>,X<0,3>,Y<1,3>,Y<4,3>>"
# ./compile "X<0,3>"

(echo "#define FORMULA $1" ; cat link_autodiff.cu) | nvcc -x cu -std=c++11 -Xcompiler -fPIC -shared -o /dev/stdout -I $PWD - > "$1.so"
